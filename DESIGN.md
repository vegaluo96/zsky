# 《摘星》ZSky · 设计文档 v0.6

> 一句话概念：**两个人养一片星空——各自忙碌时星光在悄悄积累，每次打开都能摘一颗星送给 Ta；想 Ta 了就戳一戳，每一次想念都有记录；和情侣朋友们的星空拼成同一片星域，交相闪烁。**
> Slogan：**为你摘星**（zsky.com 域名即品牌：Z=摘，Sky=星空）

- 载体：手机竖屏优先的 H5 网页游戏（桌面浏览器同样可玩），域名 **zsky.com**，免安装点链接即玩
- 品类：放置挂机 + 异步社交
- 社交模型：**情侣为原子单位，星域（社区）为放大器**
- 核心隐喻：在一起 = 一颗**双星**（两颗星互相绕转、永不分离）

---

## 1. 目标用户与场景

- 有伴侣、手机不离身的年轻用户；微信里传播链接
- 典型场景：通勤/睡前打开 2-5 分钟，收星光、给 Ta 摘星、戳一戳想念、看星域邻居的星语
- 单次会话 2-5 分钟，每日 1-3 次

## 2. 核心循环

```
离线：星空按速率自动积攒星光（服务器时间戳结算，封顶 12h）
  ↓
上线：收集星光 → 点亮星座区域 / 升级观星台 / 解锁星域角落
  ↓
夜空越来越璀璨 → 摘一颗星挂在 Ta 的天空 / 戳一戳表达想念
  ↓
每日双人任务 / 星域任务 / 每晚黄金时刻 → 明天还想来
```

## 3. 社交设计

### 3.1 情侣层（P1）

- **绑定**：A 创建星空 → 生成邀请链接 `zsky.com/i/xxxx` → 伴侣打开即绑定，无需注册
- **在线感知**：连接即在线——状态栏显示"Ta 正在望着星空"呼吸绿点；同时在线时所有互动特效实时到达
- **异步互动**（全程不要求同时在线）：
  - **摘星**：从自己的星空摘一颗星挂到对方夜空（核心浪漫动作，星的形态=心意等级；Ta 在线时星星即时落入 Ta 的夜空）
  - **想念 · 戳一戳**（⭐核心体验）：Ta 的星空页常驻"想 Ta 了"按钮——自己一颗流星飞出；**Ta 同时在线时（SSE 实时推送）立刻看到流星划过夜空 + 星空轻颤 + "Ta 正在想你"浮层**；Ta 不在线则转为"未读想念"，**上线时逐条回放**（错过的流星依次划过，仪式感拉满）。**每次想念写入永久时间线**（可回看"今天 Ta 想了我 3 次"），纪念日自动生成金句："这一年，我们互相想念了 217 次"
    - 红线：戳一戳**不产出任何资源**——想念是情感不是任务
    - 防骚扰：戳无限免费，但推送提醒限频（1 小时至多 1 次）
  - **星语瓶**：短句 + 贴纸模板留言，漂在对方夜空里，**6 小时后才能开启**（延迟惊喜，制造回归理由）
  - 每日双人任务：各自完成才结算——天然的互相提醒
  - **纪念日系统**：在一起第 100 天触发**流星雨/极光夜** + 想念金句（截图传播点）
  - 连续共同签到 → 星空越来越清澈、星更亮（断签不惩罚，只是蒙尘）
- **防死局铁律**：对方不玩，单人可 100% 完整游玩；双人互动为 +30% 产出加成，不设双人门槛内容

### 3.2 星域层（P2 · 社区）

- 每对情侣一片星空，**4-8 对情侣**的星空拼在同一片星域，星图上彼此可见
- **互助**：串门点亮别人星空的暗角、赠送多余星光、加速对方的观星台升级
- **共建北极星**：全星域攒星光合力点亮北极星，完工全员永久加成——"为迷路的人指路"
- **点灯人榜**（周榜）：比谁最常点亮别人的星空——只做正向竞争，不做产出比较
- **星域任务**：每周集体目标（星域总亮度达标全员奖励）
- **隐私边界**：夜空、摘星、星语与想念时间线为情侣私密；星域公共层只展示亮度与互助痕迹

## 4. 账号与身份

**原则：零门槛进入，身份渐进加固。P1 已定：免注册，自定义找回码确认身份。**

| 层级 | 时机 | 方案 |
|---|---|---|
| 默认身份 | 进入即有 | 设备 token（localStorage）即账号，点开链接就能玩 |
| 找回码 | 首次进入引导设置 | 自定义字符串，换机/换浏览器凭码找回 |
| 账号绑定 | 可选 | 用户名 + 密码（多设备并存，防丢） |
| 手机验证码 | P2 可选 | 阿里云短信（个人可开通，约 4.5 分/条，签名审核 2-3 天） |
| 微信登录 | P3 | **个人小程序壳**：同一套后端 API 套小程序客户端，天然获得微信身份（openid）。个人可免费注册小程序 |

> 微信登录的现实约束：网页版微信扫码登录需**企业主体**开放平台账号；微信内 H5 授权需**认证服务号 + 备案域名**（HK 服务器无法备案）——网页端短期均不可行，小程序是唯一个人可达路径。
>
> 通知触达（"Ta 想你了"推送）：P2 做 PWA + Web Push（安卓直接支持；iPhone 需"添加到主屏幕"），接近 App 的提醒体验。

## 5. 留存设计（情感 × 机制双轮）

**北极星指标：双人共同留存**——"一对情侣都回来了"才算留存。单边指标是虚荣指标。

**分级目标**：Hook 率（绑定后 60 秒内完成第一次戳一戳/摘星）≥ 80%；D1 ≥ 40%、D7 ≥ 20%、D30 ≥ 10%（休闲 H5 及格线，验证后再上调）。

### 5.1 短留存 · 首次会话（前 5 分钟定生死）

- **5 秒给甜头**：进入即见流星划过 + 首批星光到手，先爽再教
- **引导即钩子**：新手引导只教一件事——戳一戳（教的就是留存钩子本身）
- **第一晚必有流星夜**：脚本化事件（伪随机保底），首日体验锚定最高点
- 首次摘星送出时，特效规格拉满（这是截图传播的第一现场）

### 5.2 中留存 · 约定与开放环（D1-D7）

- **每晚 21:00-23:00 许愿黄金时刻**：星光 ×2，大概率两人同时在线——实时特效的主舞台
- **流星雨预报**：提前预告"明晚 20:30 英仙座流星雨"——约定式回归（我知道明晚有值得回来的理由）
- **星语瓶延迟开启**（6h）与**每日星象签**：未完成感 + 轻好奇，每日两次轻回归理由
- **神秘流星**：随机带来小礼物——变率奖励，不可预测的惊喜
- **双人周契约**：本周合力点亮一个星座——社交义务是最强的回归理由

### 5.3 长留存 · 沉淀与身份（D30+）

- **星历（星空传记）**：自动记录你们的故事——"第 47 天，你们点亮了天鹅座；Ta 想了你 12 次"。**情感账本 = 离开成本**，也是纪念日素材库
- **星等成长体系**：暗星 → 亮星 → 巨星 → 超新星（可见的身份与炫耀层）
- **收集图鉴**：星座 / 流星形态 / 星语贴纸（长线目标）
- 纪念日经济：100 天 / 周年自动大事件 + 想念金句

### 5.4 回流设计（win-back · 铁律：不惩罚、不绑架）

- 离开后星空**蒙尘但永不消失**："你的星空等了你 30 天，Ta 的星还亮着"——惋惜感可以，负罪感不行
- **回流仪式**：回归当晚一场流星雨 + 离开期间错过的想念一次性回放
- 任何断签/离开不扣产出、不清进度、不掉排名

## 6. 数值框架（概要）

- 离线星光 = `产出速率 × 离线时长`，封顶 12h（引导每日回访，防挂死）
- 产出速率 = 各星座区产出之和 × 星象系数 ×（双人活跃 +30%）×（星域加成 ≤10%）
- **星象**（天气系统）：晴夜 ×1.2、薄云 ×1.0、多云 ×0.8；黄金时刻 ×2（21:00-23:00）；随机"流星夜"（限时收集狂欢）、纪念日"极光夜"
- 解锁曲线（**无天数门控，星光即门票**）：星座与观星台随时可用星光点亮/升级；流星宝宝常驻夜空；双星在总等级 6（观星台+星座）时成形并可命名
- 防作弊：一切数值服务器端计算，客户端纯展示；资源变动全部落库

## 7. 界面与特效精品标准

### 7.1 界面结构（竖屏）

```
┌─────────────┐
│ 星象/在线状态 │  ← Ta 在线呼吸绿点
│             │
│   我的星空    │  ← 四层视差夜空
│ （点击交互）   │
│             │
│ [Ta的星][星域] │
└─────────────┘
```

- 触控目标 ≥ 44px；音效默认关（夜晚白噪音可选）

### 7.2 特效精品三定律

1. **真实天文细节**：恒星按真实色温着色（O/B 蓝 → A/F 白 → G 黄 → K/M 橙红，直接用 BAFGKM 光谱序）、月相变化、银河淡带——真实感是廉价素材买不到的高级感
2. **物理曲线**：一切运动带缓动与引力（流星是弧线+拖尾粒子，经过时照亮邻近星）；**禁止直线/匀速动画**；交互反馈 150-250ms、仪式特效 400-800ms、绝不打断玩家操作
3. **触觉与光感**：`navigator.vibrate` 震动反馈（被戳时轻颤）；按压缩放微交互；统一一套 glow 光晕语言与色板（靛蓝 → 紫罗兰），全游戏只此一套光

### 7.3 渲染实现标准

- 四层视差：远景星云 / 远星 / 近星 / 前景流星（陀螺仪或触摸轻移时层间速差）
- 静态星空层**离屏 Canvas 预渲染**，仅动态元素增量绘制；粒子对象池复用（零 GC 卡顿）
- `devicePixelRatio` 适配渲染——Retina 锐利度，杜绝模糊是精品感的底线
- 目标 60fps，低端机优雅降级 30fps（减粒子不减帧率稳定）

## 8. 技术方案与性能预算

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | 原生 HTML/JS + CSS 动画，Canvas 用于星空渲染 | 无构建步骤，改完即生效 |
| 后端 | Node.js (Express) + better-sqlite3 | HTTP + **SSE 实时推送**（不需要 WebSocket） |
| 部署 | nginx 静态 + `/api` 反代 Node，systemd 托管 | 复用现有 deploy 链路 |

- 身份：设备 token（默认）→ 找回码 / 账号密码（渐进绑定）；情侣/星域靠邀请链接绑定
- **实时通道（SSE）**：客户端动作走普通 POST；服务端事件（戳一戳/摘星特效、在线状态）通过 SSE 单向实时推送——EventSource 自动重连，天然适配手机网络切换与锁屏；nginx 配 `proxy_buffering off` + 长 `proxy_read_timeout`；服务端每 25s 心跳注释帧保活
- **连接即在线**：SSE 连接表天然就是在线状态（presence），无需额外心跳上报
- 服务器 2C4G 富余极大（数千日活无压力）

### 8.1 性能预算（特效与速度的冲突在此裁决）

- **首开总体积 < 100KB（gzip）**：JS < 50KB、CSS < 15KB、**零图片、零自定义字体**（全程序化生成 + 系统字体）；超预算必须砍，不许放行
- **首屏可交互 < 2s**（中端手机 × 4G）；TTI 达成后才加载音效、图鉴等次要模块
- **分层加载**：CSS 渐变夜空即刻铺底 → Canvas 粒子 hydrate → 次要模块 lazy
- **乐观渲染**：localStorage 存星空快照，秒开时先渲染上次的星空再与服务器对账（感知速度 ≈ 0）
- **Loading 即氛围**：加载动画本身就是"夜幕降临"渐变——等待时间变成内容
- nginx 开 gzip + HTTP/2；P2 加 Service Worker 缓存，二次打开 < 0.5s

## 9. 分期路线

| 阶段 | 内容 | 验证目标 |
|---|---|---|
| P1 | 核心循环、14 天曲线、绑定/邀请、摘星、戳一戳（SSE 实时特效+回放）、星语、在线感知、**留存三件套（黄金时刻/星语瓶延迟/星历）+ 回流不惩罚** | 一对情侣能否连玩 14 天；Hook 率 ≥ 80% |
| P2 | 星域、互助、共建北极星、点灯人榜、PWA+Web Push、（可选）短信、星等与图鉴扩展 | 是否自发拉其他情侣进星域；D7 ≥ 20% |
| P3 | 节日星象、双星命名纪念品、星空皮肤、个人小程序壳（微信登录） | 传播与（可选）商业化 |

## 10. 风险清单

1. **内容消耗**：放置游戏 3-7 天数值见底即流失 → 14 天曲线 + 黄金时刻/流星雨预报做节奏锚点，长线靠星域与节日
2. ~~美术量~~ → 程序化生成 + 真实天文细节兜底精品感；风险转移为"粒子性能"（离屏渲染 + 对象池 + 帧率降级，见 7.3/8.1）
3. **特效 vs 速度** → 以性能预算硬约束（<100KB / <2s），特效在预算内做加法，超了先砍体积不砍品质底线
4. **情侣单边弃坑**：单人可玩兜底 + 伴侣位转"AI 星伴"（P2 后评估）+ 回流不惩罚设计
5. **星域冷启动**：星域玩法不设门槛，一对情侣也完整可玩
6. **通知触达弱**（H5 无推送）→ P2 PWA + Web Push 补齐；小程序壳（P3）彻底解决
7. **长连接管理**：手机锁屏/网络切换导致 SSE 断开 → EventSource 自动重连 + 服务端按事件 id 幂等去重（不丢不重的想念）；进程重启同理恢复

---
*v0.6 · 2026-09-14 · 品质升级：新增留存设计专章（情感×机制双轮、北极星=双人共同留存、回流不惩罚）、特效精品三定律与渲染标准、性能预算硬约束（<100KB/<2s）。*
